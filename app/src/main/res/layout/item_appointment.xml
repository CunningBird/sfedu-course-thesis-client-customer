<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/cv_user_img"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginVertical="15dp"
        android:layout_marginStart="20dp"
        android:layout_centerInParent="true"
        android:innerRadius="0dp"
        android:shape="ring"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_appointment_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:paddingHorizontal="20dp"
        android:maxLines="1"
        style="@style/meesageUserName"
        android:text="[Appointment name]"
        app:layout_constraintStart_toEndOf="@id/cv_user_img"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_appointment_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="20dp"
        android:maxLines="1"
        style="@style/meesageUserDescription"
        android:text="[appointment time]"
        app:layout_constraintStart_toEndOf="@+id/cv_user_img"
        app:layout_constraintTop_toBottomOf="@+id/tv_appointment_name" />
</androidx.constraintlayout.widget.ConstraintLayout>